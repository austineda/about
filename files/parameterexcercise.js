// Create a function that takes in a parameter.
// If the parameter is bool flip the bool, if the parameter is a number making it opposite, if string, reverse the string

function flip(value){
	if(typeof(value) === 'boolean'){
		let b = true;
		// ! returns the opposite value
		b = !b;
//		if(value === true){
//			return false
//		}	else {
//			return true
//		}
	}
	
}


console.log(value);