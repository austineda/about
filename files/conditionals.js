// Conditionals are blocks of code that are only executed
// if a certain condition evaluates to true

let score = -2;
if(score == 0){
    console.log('no points have been scored.');
} else if(score > 0 && score < 10){
    console.log('keep playing');
} else {
    console.log('the game is over');
}
