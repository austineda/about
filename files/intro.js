// console.log prints to the terminal    
console.log('My name is Austin')

//string type: These are going to be your letters, sentences, etc.
let name = 'Austin Castaneda'
console.log(name)

// number type: There are numbers, double precision
let number = 6548
console.log(number)

// boolean type: this is for true/falese values
let done = false; 
console.log(done)

done = true;
console.log(true)
// Null Type: Nothing is being stored with this variable

let nulltype = null 
console.log(nulltype)

// Const declares a variable that cannot be changed
const permaname = "Austin Castaneda"

console.log(permaname)

// An epression is a string of a single value

//A statment is a single instruction that uses or manipulates values

//Assignment (=) is the process of storing a value within a variable
// Comparison (==) determines if the value on the left side is equivalent towhat is on the right

//objects are a complilation of variables that define a thing

let myHome = 'Washington';
let vacation = 'Europe';

let cat = {
    breed: 'tabby',
    color: 'orange',
    age: 5,
    isBlind: true,
    };
console.log(cat)
console.log(cat.color)

cat.color = 'naranja'

console.log(cat)


//Arrays: Data structure that stores information in contiguous memory

//Arrays have indices that start at 0
let dogModels = [
    golden_retriever = {
        color: 'gold',
        personality: 'very good boys',
        size: 'grande',
    },
    bulldog = {
        color: 'many',
        personality: 'lazy',
        size: 'pequeno',
    },
    Chihuaha = {
        color: 'many',
        personality: 'fiesty',
        size: 'muy pequeno',
    },
];


console.log(dogModels)

console.log(dogModels[2].personality)

// Without iterators, we would have to print each index in an array individually
//basic for loop
//1. Starting point
//2. Endpoint
